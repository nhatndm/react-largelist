import { createContext } from "react";

export const CalendarTableContext = createContext();

export const CalendarTableContextProvider = CalendarTableContext.Provider;
export const CalendarTableContextCosumner = CalendarTableContext.Consumer;

export const CalendarContext = createContext();

export const CalendarContextProvider = CalendarContext.Provider;
export const CalendarContextCosumner = CalendarContext.Consumer;
